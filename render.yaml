services:
  # File: render.yaml
  # One Render Web Service builds the React app and runs Express, so the frontend
  # and API are available from the same domain.
  - type: web
    name: evently
    runtime: node
    plan: free
    # Vite is a development dependency but is required to create the production frontend build.
    buildCommand: npm install --prefix backend && npm install --include=dev --prefix frontend && npm run build --prefix frontend
    startCommand: npm start --prefix backend
    envVars:
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: EMAIL_USER
        sync: false
      - key: EMAIL_PASS
        sync: false
      - key: NODE_ENV
        value: production
